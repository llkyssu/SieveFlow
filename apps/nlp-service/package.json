{
  "name": "nlp-service",
  "version": "0.0.1",
  "private": true,
  "description": "NLP Service for CV parsing and analysis using FastAPI",
  "scripts": {
    "dev": "cd src && uvicorn main:app --reload --host 0.0.0.0 --port 8001",
    "start": "cd src && uvicorn main:app --host 0.0.0.0 --port 8001",
    "install": "pip install -r requirements.txt",
    "test": "pytest"
  }
}
